#SVGDraw
An engine for producing printable and cuttable SVGs and experimenting with generative art

To create a new module, run the following command in the project directory:
`npm run new-module -- my-module "My Feature Label"`
